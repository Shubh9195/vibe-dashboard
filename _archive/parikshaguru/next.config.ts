import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Move turbopack to the root of the config
  turbopack: {
    root: "C:/Users/ASUS/vibe-dashboard/parikshaguru",
  },
};

export default nextConfig;
